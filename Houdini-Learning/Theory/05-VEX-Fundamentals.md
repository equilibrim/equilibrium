# Основы VEX в Houdini

> Уровень: 🟠 Средний  
> Контекст: SOP → Attribute Wrangle  
> Время: 1.5–2 часа  
> Зависит от: [[03-Geometry-Types.md]]
> 💡 VEX используется для анализа overhangs в 3D-печати: [[02-3D-Printer]]

---

## 🎯 Цель
Научиться использовать **VEX** для:
- чтения и записи атрибутов (`@P`, `@Cd`, `@N`),
- применения математики и шума,
- управления геометрией через код.

---

## 🔑 Ключевые идеи

### 1. VEX — это не «полноценный» язык программирования
- Он **декларативный** и **параллельный**.
- Код выполняется **независимо для каждой точки/примитива**.

### 2. Четыре контекста выполнения
| Контекст | Когда использовать |
|---------|------------------|
| `point` | Изменение позиции, цвета точек |
| `vertex` | Работа с UV, интерполяцией |
| `primitive` | Масштабирование, удаление граней |
| `detail` | Глобальные вычисления (например, центр массы)

---

### 3. Встроенные атрибуты (доступны без объявления)
```c
@P      // позиция (vector)
@Cd     // цвет (vector)
@N      // нормаль (vector)
@ptnum  // номер точки (int)
@Time   // глобальное время (float)

// Поднять все точки на 1 единицу
@P.y += 1;
// Случайный цвет
@Cd = rand(@ptnum + 100);
// Шумовое смещение
@P += noise(@P * 3) * 0.1;
// Удаление точек
if (@P.y < 0) removepoint(0, @ptnum);
// Цвет по высоте
@Cd = set(fit(@P.y, 0, 2, 0, 1), 0.5, 0.8);
```
---

## 🛠️ Как начать

1. Создайте геометрию (Box, Grid).
2. Добавьте Attribute Wrangle (**Tab** → **attribwrangle**).
3. Выберите **Run Over** → **Point**.
4. Пишите код → смотрите результат в реальном времени.
> 💡 Совет: используйте **Geometry Spreadsheet**, чтобы видеть изменения атрибутов.

## 📚 Ресурсы

- [SideFX:VEX Quick Reference](https://www.sidefx.com/docs/houdini/vex/snippets.html?spm=a2ty_o01.29997173.0.0.715d5171H6RN8n)
- [Applied Houdini — Scene 4: VEX Basics](https://www.sidefx.com/tutorials/applied-houdini-scene-4-vex-basics/?spm=a2ty_o01.29997173.0.0.715d5171H6RN8n)

## ✅ Критерии завершения
- [ ] Создан хотя бы один рабочий `Wrangle`
- [ ] Использованы `@P`, `@Cd`, `rand()`
- [ ] Понят разница между point и detail контекстами
- [ ] Заметки в notes.md
- [ ] Прогресс обновлён